# 사주랩 릴스 제작 규칙 (반복 제작용 표준)

`scripts/reel-template/` 시스템의 실제 구현 사양을 그대로 문서화한 표준입니다. R25(최초 수작업 버전)의 스타일을 검증된 기준으로 삼아 그대로 고정했고, 이후 만드는 모든 릴스는 이 규칙을 따르면 톤·구조가 자동으로 통일됩니다.

## 1. 영상 길이 — 16.2초 고정

5장면 × 정해진 초 수를 크로스페이드(0.2초씩 겹침)로 이어붙입니다.

| 장면 | 길이 | 누적 시점 |
|---|---|---|
| ① HOOK | 2.0초 | 0~2초 |
| ② INFO | 5.0초 | 2~7초 |
| ③ CURIOSITY | 3.0초 | 7~10초 |
| ④ 실제 앱 화면 | 3.0초 | 10~13초 |
| ⑤ CTA | 4.0초 | 13~17초 |

전환 겹침(0.2초 × 4회) 반영 총 길이 = **16.2초**. 길이를 바꾸고 싶으면 `build.mjs`의 `D = [2, 5, 3, 3, 4]` 배열만 수정하면 전체 타이밍(줌 속도, 오디오 싱크)이 자동으로 재계산됩니다.

## 2. 화면 구성 — 5장면 고정 구조

1. **HOOK**: 스크롤을 멈추는 첫 문장. 배경만, 텍스트 하나
2. **INFO**: 카테고리 라벨 + "일반 설명(회색) → 핵심 키워드(금색, 가장 큼) → 마무리(흰색)" + 보조 설명 2줄
3. **CURIOSITY**: "나는 어떨까?" 식 연결 문장
4. **실제 앱 화면**: 진짜 서비스 스크린샷(카드 프레임 안에 배치) + 캡션 한 줄 — *설명이 아니라 실제 제품을 보여줘서 신뢰도를 만드는 장면*
5. **CTA**: "OO운 궁금증, 풀렸나요?" → 메인 헤드라인 → 버튼형 강조 박스

장면 전환은 크로스페이드(0.2초) + 은은한 줌인(Ken Burns, 장면마다 최대 확대율이 1.03배→1.06배로 점점 커짐)만 사용합니다. 화려한 트랜지션은 쓰지 않습니다 — 텍스트가 주인공이라는 원칙.

## 3. 자막 스타일

| 요소 | 값 |
|---|---|
| 캔버스 | 1080×1920 (9:16), 30fps |
| 배경 | 남색 그라데이션 `#0e0b1f → #171331 → #0e0b1f` (135도) |
| 강조색 (골드) | `#d4af6a` — 핵심 키워드, CTA 헤드라인, 버튼 테두리 |
| 본문색 (아이보리) | `#f3efe8` — HOOK, 마무리 문장 |
| 보조색 (뮤트 라벤더) | `#b9b3d6` — 카테고리 라벨, 서브 설명 |
| HOOK 폰트 크기 | 84px, bold |
| INFO 강조 키워드 | 72px, bold, 골드 |
| CTA 헤드라인 | 88px, bold, 골드 |
| 정렬 | 항상 중앙 정렬 |

**세이프존(중요)**: 상단 약 300px·하단 약 400px는 텍스트를 배치하지 않습니다(Reels UI의 캡션·유저네임·좋아요/댓글 아이콘과 겹치지 않게). 텍스트 최대 폭은 좌우 100px씩 여백을 둔 **880px**로 제한합니다.

**한 줄 글자 수 규칙(실측 기준)**: HOOK처럼 큰 폰트(84px)에서 한 줄에 **한글 10자 이상**이 들어가면 화면 폭을 넘어서 줄바꿈이 깨집니다(예: "요"만 다음 줄로 밀림). **한 줄당 한글 9자 이하**를 기준으로 잡고, 넘으면 줄을 하나 더 나누세요. HOOK은 보통 3~4줄로 구성됩니다.

## 4. 브랜드 요소 (모든 장면 공통)

- 좌상단 고정: 원형 배지("四") + "사주랩" 텍스트 — 모든 릴스, 모든 장면에 항상 노출
- 배경에 옅은 링 장식 2개(반경 500px·380px, 투명도 10%·16%) — 미니멀하게 유지, 과하게 채우지 않음

## 5. 첫 장면(HOOK) 작성 원칙

- **분량**: 3~4줄, 줄당 9자 이하
- **톤**: 궁금증(정보를 일부러 끊기) · 공감(구체적 상황 지목) · 반전("~라는데 사실은") 중 하나 이상 사용
- **금지**: "당신의 사주가 궁금하다면" 같은 평범한 광고 문구, 확정적 예언 표현("~하게 됩니다"), 공포 조장 표현
- 카테고리별 예시와 30개 전체 대본은 [INSTAGRAM_REELS_30.md](INSTAGRAM_REELS_30.md) 참고

## 6. CTA 위치와 구성

- **위치**: 항상 마지막 장면(13~17초 구간), 영상 전체 길이의 마지막 4초
- **구성 3단**: ① 방금 다룬 카테고리를 다시 언급하는 연결 문구(뮤트색, 작게) → ② 메인 헤드라인 "내 사주는 어떨까?"(골드, 크게) → ③ 버튼형 박스 "프로필 링크에서 무료 확인 🔮"(테두리 강조)
- 광고처럼 들리지 않게, "다음 행동 안내"처럼 느껴지도록 구성 — 상품명이나 가격은 릴스 안에서 언급하지 않음(무료 확인까지만 유도, 유료 전환은 앱 안에서 처리)

## 7. 오디오 — 100% 자체 합성 (저작권 이슈 없음)

모든 배경음·효과음은 `ffmpeg`의 `sine`/`anoisesrc` 필터로 직접 만듭니다. 외부 음원을 전혀 쓰지 않아 저작권 걱정이 없습니다.

- 코드 진행 2세트(A마이너→F메이저 느낌의 3화음)가 6초 지점에서 크로스페이드
- 장면 전환마다 짧은 whoosh 효과
- HOOK 시작 시 아르페지오, CTA 시작 시 ding 효과음
- 마스터링: 에코 + Haas 스테레오 확장 + 리미터로 자연스러운 공간감

이 오디오 필터 체인은 `build.mjs`의 `buildAudioFilter()`에 고정돼 있어 새 릴스를 추가해도 자동으로 동일하게 적용됩니다.

## 8. 업로드 형식

| 항목 | 값 |
|---|---|
| 컨테이너 | mp4 (H.264 + AAC) |
| 비율 | 9:16 세로 |
| 오디오 | 44.1kHz, 160kbps |
| 특수 옵션 | `faststart` (모바일에서 즉시 재생 시작) |
| 빌드 산출물 파일명 | `{2자리순번}_{ID}_{카테고리영문코드}.mp4` (예: `01_R01_jaemul.mp4`) |
| 업로드 폴더 파일명 | `{2자리순번}_{ID}_{한글카테고리}_{제목요약}.mp4`로 리네임해서 `인스타그램_릴스_업로드순서/`에 정리 |

카테고리 영문 코드: 재물운=`jaemul`, 연애운=`yeonae`, 직업운=`jigeop`, 생년월일=`saengnyeon`, 사주상식=`sangsik`

## 9. 반복 제작 절차 (새 릴스 1개 추가하는 법)

1. [INSTAGRAM_REELS_30.md](INSTAGRAM_REELS_30.md)에서 대본(HOOK/INFO/CURIOSITY/CTA/캡션/해시태그) 확인 또는 새로 작성 — 6번 원칙 지켜서 HOOK 작성
2. `scripts/reel-template/content.mjs`의 `REELS` 배열에 새 항목 추가 (hook을 9자 이하 줄로 분리, order 번호 지정)
3. `node scripts/reel-template/content.mjs`로 문법 확인(선택) 후 `node scripts/reel-template/build.mjs` 실행 → `/reels` 폴더에 mp4 생성
4. 생성된 파일을 `인스타그램_릴스_업로드순서/`로 복사하며 8번 규칙대로 리네임
5. 업로드 시 [INSTAGRAM_REELS_30.md](INSTAGRAM_REELS_30.md)의 해당 캡션·해시태그를 그대로 사용

이 5단계만 반복하면 사람이 매번 디자인·타이밍·오디오를 새로 만들 필요 없이, 대본(텍스트)만 바꿔서 계속 같은 톤의 릴스를 찍어낼 수 있습니다.
