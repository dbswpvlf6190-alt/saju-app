# 사주랩 마케팅 핵심 지표(KPI) 정의

Reels 조회수부터 결제 성공까지, 그리고 공유 횟수까지 총 10개 지표를 어디서·어떻게 측정하고, 단계별 전환율을 어떤 공식으로 계산하며, 어느 수준이면 "정상"이고 어느 수준이면 "개선이 필요한지" 기준을 정리합니다. [FUNNEL_OPTIMIZATION.md](FUNNEL_OPTIMIZATION.md)의 퍼널 진단과 [VIRAL_SHARE_STRATEGY.md](VIRAL_SHARE_STRATEGY.md)에서 새로 구현한 `source`별 공유 트래킹을 그대로 이어받아, 실제로 매주 채워 넣을 수 있는 지표표로 만들었습니다.

## 0. 측정 소스 3가지

| 소스 | 포함 지표 | 비고 |
|---|---|---|
| **Instagram Insights** | 조회수, 도달, 프로필 방문, (스토리/하이라이트) 링크 클릭 | 앱 코드로는 측정 불가능한 외부 지표. 프로필 bio 링크의 클릭수는 인스타그램이 따로 제공하지 않아 **`landing_view`의 `ref=ig_profile` 건수로 근사**([FUNNEL_OPTIMIZATION.md](FUNNEL_OPTIMIZATION.md) 3번에서 이미 붙이기로 한 파라미터) |
| **`AnalyticsEvent` 테이블**(자체 DB) | 무료 사주 시작(`saju_start`), 결과 완료(`saju_complete`), 상세분석 CTA 클릭(`premium_cta_click`), 결제 시작(`checkout_start`), 결제 성공(`payment_success`), 공유 횟수(`share_click`) | `src/lib/analytics/events.ts`에 정의된 실제 이벤트명. 세션 식별자가 없어 **건수 비율로 근사 전환율**을 낸다(정밀 코호트 아님 — [FUNNEL_OPTIMIZATION.md](FUNNEL_OPTIMIZATION.md) 0번과 동일 전제) |
| **`Order` 테이블**(자체 DB) | 결제 성공 건수·매출 검증용 | `/admin` 대시보드에서 바로 확인 가능(결제완료 건수·총 매출) — `payment_success` 이벤트와 대조해 트래킹 누락이 없는지 교차 확인하는 용도 |

---

## 1. 10개 지표 정의

| # | 지표 | 정의 | 측정원 | 집계 방법 |
|---|---|---|---|---|
| ① | Reels 조회수 | 게시물 재생 횟수(재시청 포함) | Instagram Insights | 콘텐츠별 "조회수" 값, 30일 합산 |
| ② | 도달률 | 콘텐츠를 본 **고유 계정 수**가 조회수 대비 얼마나 되는지 | Instagram Insights("도달") | 도달 계정 수 ÷ 조회수 |
| ③ | 프로필 방문 | 콘텐츠를 보고 프로필까지 이동한 횟수 | Instagram Insights(프로필 활동) | 계정 인사이트의 "프로필 방문" 값 |
| ④ | 링크 클릭 | 프로필의 앱 링크를 실제로 누른 횟수 | `landing_view`(`ref=ig_profile` 또는 `ig_r{릴스ID}`) | `AnalyticsEvent` 카운트 쿼리 |
| ⑤ | 무료 사주 시작 | 생년월일시 입력 폼 제출 | `saju_start` | `AnalyticsEvent` 카운트 |
| ⑥ | 결과 완료 | 사주 계산 성공, 결과 화면 진입 | `saju_complete` | `AnalyticsEvent` 카운트 |
| ⑦ | 상세분석 CTA 클릭 | "4,900원으로 전체 분석 보기" 버튼 클릭(가격 최초 인지 시점) | `premium_cta_click` | `AnalyticsEvent` 카운트 |
| ⑧ | 결제 시작 | PortOne 결제창 호출 | `checkout_start` | `AnalyticsEvent` 카운트 |
| ⑨ | 결제 성공 | 실제 결제 완료 | `payment_success` (+ `Order.status=PAID`로 교차 검증) | `AnalyticsEvent` 카운트 또는 `/admin` 결제완료 건수 |
| ⑩ | 공유 횟수 | 카카오톡/SNS 공유 버튼 클릭 | `share_click`(이제 `meta.source`로 `free_result`/`compat_free`/`premium_unlocked` 구분됨) | `AnalyticsEvent` 카운트, `source`별 그룹핑 |

**공통 조회 쿼리**(Vercel Postgres 콘솔 또는 `npx prisma studio`):
```sql
select name, metaJson, count(*)
from "AnalyticsEvent"
where "createdAt" >= now() - interval '7 days'
group by name, metaJson
order by count(*) desc;
```
`metaJson`으로 그룹핑하면 `ref`값별(④번), `source`값별(⑩번) 세부 집계가 한 번에 나옵니다.

---

## 2. 단계별 전환율 계산 공식

| 구간 | 공식 | 의미 |
|---|---|---|
| 도달률 | ② ÷ ① | 조회 중 실제 고유 시청자 비율(재시청이 많을수록 낮아짐 — 낮다고 나쁜 게 아니라 팬덤 신호일 수도 있음) |
| 프로필 방문율 | ③ ÷ ② | 콘텐츠가 "더 알아보고 싶다"는 반응을 만들었는지 |
| 링크 클릭율 | ④ ÷ ③ | 프로필에서 실제 행동까지 이어졌는지(bio 문구·하이라이트 배치의 힘) |
| 랜딩 → 시작율 | ⑤ ÷ ④ | 링크를 눌러 들어온 사람이 실제로 입력을 시작하는 비율 |
| 입력 완료율 | ⑥ ÷ ⑤ | 입력 폼 자체의 이탈률(항목 수·난이도) |
| CTA 클릭율 | ⑦ ÷ ⑥ | **가격 저항 vs 가치전달**을 가르는 가장 중요한 구간([FUNNEL_OPTIMIZATION.md](FUNNEL_OPTIMIZATION.md) ⑧번과 동일) |
| 결제 시작율 | ⑧ ÷ ⑦ | CTA 클릭이 진짜 구매 의도였는지 |
| 결제 성공율 | ⑨ ÷ ⑧ | PG 결제창 자체의 이탈(마케팅보다 결제 UX/카드사 이슈 영역) |
| **총전환율(핵심 KPI)** | ⑨ ÷ ① | Reels 조회 1건이 실제 매출로 이어지는 최종 비율 — 이 문서에서 가장 자주 볼 숫자 |
| 공유율 | ⑩ ÷ (⑥+결제 완료 화면 노출 수) | 결과를 본 사람 중 실제로 공유한 비율 |
| 공유 재유입율(바이럴계수) | `landing_view`(`ref`가 `share_`로 시작) 건수 ÷ ⑩ | 공유 1건이 실제 새 방문을 몇 명 만드는지(1.0 이상이면 순수 공유만으로 방문자가 늘어나는 구조) |

---

## 3. 개선 기준 — 3단계 신호등

아래 수치는 실측 데이터가 없는 **초기 판단 기준(가설)**입니다. 국내 인스타그램 릴스·프리미엄 전환 퍼널의 일반적인 범위를 기준으로 잡았고, [30DAY_UPLOAD_SCHEDULE.md](30DAY_UPLOAD_SCHEDULE.md)의 Day 10/14/20 체크포인트에서 실측치로 교체해 재조정하는 것을 전제로 합니다.

| 구간 | 🟢 정상 | 🟡 관찰 필요 | 🔴 즉시 개선 |
|---|---|---|---|
| 도달률(②÷①) | 70%↑ | 50~70% | 50% 미만(같은 사람이 계속 재생 — 신규 도달 부족) |
| 프로필 방문율(③÷②) | 3%↑ | 1.5~3% | 1.5% 미만 → HOOK은 되는데 CTA/궁금증이 약함 |
| 링크 클릭율(④÷③) | 25%↑ | 12~25% | 12% 미만 → bio 문구·하이라이트 점검 |
| 랜딩→시작율(⑤÷④) | 45%↑ | 25~45% | 25% 미만 → 랜딩 첫 화면 재점검 |
| 입력 완료율(⑥÷⑤) | 75%↑ | 55~75% | 55% 미만 → 입력 항목·시간모름 안내 문구 점검 |
| **CTA 클릭율(⑦÷⑥)** | 8%↑ | 4~8% | 4% 미만 → 최우선 개선 대상(가격 인지 지점) |
| 결제 시작율(⑧÷⑦) | 55%↑ | 35~55% | 35% 미만 → 입력폼 부담 재점검 |
| 결제 성공율(⑨÷⑧) | 75%↑ | 55~75% | 55% 미만 → PortOne 설정/카드사 오류 확인 |
| 공유율(⑩÷⑥) | 5%↑ | 2~5% | 2% 미만 → 공유 카피/버튼 위치 재점검 |
| 바이럴계수 | 0.3↑ | 0.1~0.3 | 0.1 미만 → 공유가 실제 재유입으로 안 이어짐 |

**우선순위**: 🔴가 여러 구간에서 동시에 뜨면, **깔때기 앞쪽(도달~프로필방문)보다 CTA 클릭율을 먼저 봅니다.** 앞쪽은 콘텐츠 물량이 쌓이면 자연히 개선되는 경우가 많지만, CTA 클릭율은 카피 문제라 계정 크기와 무관하게 지금 바로 고칠 수 있고, 매출에 가장 직접적으로 연결됩니다.

---

## 4. 목표 역산 워크시트 — 21건 결제(매출 10만원) 기준

`30DAY_MARKETING_PLAN.md`/`30DAY_UPLOAD_SCHEDULE.md`의 목표(첫 매출 10만원 ≈ 4,900원 × 21건)를 위 3번 표의 "정상" 하한값으로 역산하면 필요한 조회수 규모를 가늠할 수 있습니다. **이 숫자는 계획용 예시이지 확정치가 아닙니다** — Day 10 실측치가 나오면 반드시 이 표를 다시 계산해서 교체하세요.

| 단계 | 가정 전환율(정상 하한) | 필요 수치(역산) | 실측치(매주 기입) |
|---|---|---|---|
| ⑨ 결제 성공 | - | 21건 | |
| ⑧ 결제 시작 | 75% | 28건 | |
| ⑦ CTA 클릭 | 55% | 51건 | |
| ⑥ 결과 완료 | 8% | 638명 | |
| ⑤ 무료 사주 시작 | 75% | 850명 | |
| ④ 링크 클릭 | 45% | 1,890건 | |
| ③ 프로필 방문 | 25% | 7,560건 | |
| ② 도달 | 3% | 252,000건 | |
| ① 조회수 | 70% | 360,000회 | |

30일 동안 42개 영상(신규 30 + 앙코르 12, [30DAY_UPLOAD_SCHEDULE.md](30DAY_UPLOAD_SCHEDULE.md) 기준)에 나눠 담으면 **영상 1개당 평균 약 8,600회** 조회가 필요한 셈입니다. 신규 계정 기준으로는 낙관적인 숫자이므로, Day 10 체크포인트에서 실제 도달률·프로필방문율을 채워 넣고 이 표를 다시 계산해 현실적인 목표(또는 필요한 콘텐츠 물량 증가)를 재산정하는 용도로 쓰세요.

---

## 5. 주간 기록 시트 (그대로 복사해서 매주 채우기)

| 주차 | ①조회수 | ②도달 | ③프로필방문 | ④링크클릭 | ⑤시작 | ⑥완료 | ⑦CTA클릭 | ⑧결제시작 | ⑨결제성공 | ⑩공유 | 총전환율(⑨÷①) |
|---|---|---|---|---|---|---|---|---|---|---|---|
| 1주차 | | | | | | | | | | | |
| 2주차 | | | | | | | | | | | |
| 3주차 | | | | | | | | | | | |
| 4주차 | | | | | | | | | | | |

①~③은 Instagram Insights에서, ④~⑩은 위 1번의 SQL 쿼리로 매주 같은 요일에 채우면 4주 뒤엔 어느 구간이 계속 🔴인지 패턴이 보입니다.

---

## 6. 지금 가장 먼저 챙겨야 할 지표 Top 3

1. **CTA 클릭율(⑦÷⑥)** — 가격을 처음 인지하는 지점이라 매출에 가장 직접적. [FUNNEL_OPTIMIZATION.md](FUNNEL_OPTIMIZATION.md)에서 이미 카피 개선 방향을 제안해뒀습니다.
2. **링크 클릭율(④÷③)** — 지금까지 측정 공백이었던 구간이라, `ref=ig_profile` 파라미터를 프로필 링크에 붙이는 즉시 오늘부터 데이터가 쌓이기 시작합니다.
3. **공유 재유입율(바이럴계수)** — [VIRAL_SHARE_STRATEGY.md](VIRAL_SHARE_STRATEGY.md)에서 구현한 3개 공유 지점(`source=free_result/compat_free/premium_unlocked`)이 실제로 신규 방문을 만드는지 첫 2주 안에 확인해야, 유료 광고 없이 자연 성장이 가능한 구조인지 판단할 수 있습니다.
