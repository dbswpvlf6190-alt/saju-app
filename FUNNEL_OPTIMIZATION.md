# 사주랩 Instagram 유입 퍼널 최적화

Reels → 프로필 → 무료 사주 → 무료 결과 → 4,900원 상세 분석 → 결제까지 전체 구간을 분석합니다. **코드·결제 구조는 건드리지 않고, 마케팅 관점(카피·배치·타이밍)에서만 진단**했습니다. 실제 이벤트 트래킹 코드(`src/lib/analytics/`)와 각 화면 컴포넌트를 직접 읽고 그대로 반영했습니다.

## 0. 측정의 전제 — 지금 잴 수 있는 것 / 없는 것

`AnalyticsEvent` 테이블(`prisma/schema.prisma:51`)은 `name`, `metaJson`, `createdAt`만 있고 **세션/유저 식별자가 없습니다.** 즉 "같은 사람이 landing_view → saju_start → 결제까지 갔는지"를 1:1로 추적하는 진짜 코호트 퍼널은 지금 구조로는 안 됩니다.

대신 **같은 기간에 발생한 이벤트 건수의 비율**로 근사 전환율을 냅니다(예: 이번 주 `saju_start` 건수 ÷ 이번 주 `landing_view` 건수). 방문자 수가 아직 하루 수십~수백 단위인 초기 단계에서는 이 근사치로도 병목 구간을 충분히 짚어낼 수 있습니다. 세션 단위 정밀 측정이 필요해지는 시점(하루 방문 500+)이 오면 그때 `sessionId` 컬럼 추가를 검토하면 됩니다 — 지금은 결제/기능 변경 없이 그대로 둡니다.

**실측 쿼리 예시** (Vercel Postgres 콘솔이나 `npx prisma studio`에서):
```sql
select name, count(*) 
from "AnalyticsEvent" 
where "createdAt" >= now() - interval '7 days' 
group by name 
order by count(*) desc;
```
이 결과를 아래 단계별 지표 공식에 대입하면 됩니다.

---

## 1. 전체 퍼널 한눈에 보기

| 단계 | 이벤트/측정원 | 전환율 공식 | 지금 상태 | 병목 가능성 |
|---|---|---|---|---|
| ① Reels 노출 → 시청 | Instagram Insights (조회수) | - | 콘텐츠별 상이 | - |
| ② Reels → 프로필 방문 | Instagram Insights (프로필 활동) | 프로필 방문 ÷ 조회수 | 미측정(인스타 자체 지표만) | **높음** |
| ③ 프로필 → 랜딩 방문 | `landing_view`(ref) | landing_view(ref=share_*) ÷ 프로필 링크 클릭 | `ref` 파라미터로 유입 출처 구분 가능 | 중간 |
| ④ 랜딩 → 무료 입력 시작 | `saju_start` | saju_start ÷ landing_view | - | 낮음(폼이 바로 보임) |
| ⑤ 입력 시작 → 완료 | `saju_complete` | saju_complete ÷ saju_start | - | 낮음~중간(이탈 시 재입력 부담) |
| ⑥ 완료 → 무료 결과 열람 | `free_result_view` | free_result_view ÷ saju_complete | 거의 100%(자동 스크롤) | - |
| ⑦ 무료 결과 → 유료 미리보기 열람 | `premium_preview_view` | premium_preview_view ÷ free_result_view | 1순위 개선 완료(차별화 문구+리뷰 배치) | 중간 |
| ⑧ 미리보기 → CTA 클릭 | `premium_cta_click` | premium_cta_click ÷ premium_preview_view | 2단계 폼(intro/details) 적용됨 | **높음(가격 최초 노출 지점)** |
| ⑨ CTA → 결제 시작 | `checkout_start` | checkout_start ÷ premium_cta_click | 정보 입력 단계 | 중간(입력 이탈) |
| ⑩ 결제 시작 → 완료 | `payment_success` | payment_success ÷ checkout_start | PortOne 실연동 완료 | 낮음(PG 이탈은 마케팅 영역 밖) |

가장 큰 손실이 예상되는 구간은 **②(Reels→프로필)**와 **⑧(미리보기→CTA클릭, 가격 최초 노출)** 입니다. 이유는 각 단계에서 설명합니다.

---

## 2. 단계별 상세

### ① Reels 노출 → 시청 완료율
- **측정**: Instagram Insights의 "평균 시청 시간" / "완독률"(3초 유지율, 끝까지 본 비율)
- **개선 방향**: 이미 `CONTENT_PRODUCTION_GUIDE.md`에 HOOK 2초 규칙·9자 줄바꿈 규칙을 표준화해뒀습니다. 여기서 추가로 볼 지표는 **카테고리별 완독률**입니다 — `30DAY_UPLOAD_SCHEDULE.md`의 Day 10 체크포인트에서 "저장+공유율"뿐 아니라 **완독률도 같이 기록**하면, 어떤 카테고리가 훅은 강한데 중반(INFO~CURIOSITY)에서 이탈하는지 구분됩니다. 이탈이 3~7초 구간(INFO 장면)에 몰리면 정보량을 줄이고, 13초 이후(CTA 장면)에 몰리면 오히려 좋은 신호(이미 볼 걸 다 봤다는 뜻)입니다.

### ② Reels → 프로필 방문 (측정 공백 구간, 가장 먼저 메워야 함)
- **측정**: Instagram Insights `프로필 활동` 탭의 "프로필 방문" 수를 조회수로 나눔. 앱 쪽 이벤트로는 잡히지 않는 순수 인스타그램 내부 행동입니다.
- **왜 병목일 가능성이 높은가**: 현재 CTA는 "프로필 링크에서 무료 확인"이라는 **2단계 행동**(프로필 이동 → 링크 탭)을 요구합니다. 영상 시청자 중 실제로 프로필까지 가는 비율이 통상 1~3% 수준이면 정상, 그 아래면 CTA 자체가 약한 신호입니다.
- **개선 방법(카피/배치만)**:
  - 캡션 첫 줄에 "무료 확인 → 프로필 링크" 문구를 **캡션 최상단**에 다시 한번 반복(현재는 영상 안 CTA 장면에만 있고 캡션은 별도 확인 필요 — `INSTAGRAM_REELS_30.md`의 캡션 구조를 볼 때 이미 있는지 편차가 있을 수 있으니 30개 캡션 전수 점검 권장)
  - 프로필 bio 자체의 첫 줄에 "🔮 무료 사주 30초 확인 👇"처럼 **행동 유도 문구 + 화살표**를 배치해 "여기 누르면 된다"는 인지 부담을 줄임
  - Highlight(스토리 하이라이트)에 "무료로 보기" 커버를 만들어 프로필 도착 즉시 링크 없이도 앱 미리보기를 보여주는 것도 프로필 체류 시간을 늘리는 보조 장치가 됨

### ③ 프로필 → 랜딩 방문 (ref 트래킹 이미 구현됨)
- **측정**: `landing_view` 이벤트의 `meta.ref` 값 분포. 단, 이는 **공유 버튼발 재유입**(`share_kakao`/`share_native`/`share_copy`)만 구분하고, "인스타 프로필 링크로 처음 들어온 방문"은 `ref` 없이 그냥 `landing_view`로만 잡힙니다.
- **개선 방법(코드 변경 없이 가능)**: 프로필의 링크(Linktree든 직접 URL이든)에 `?ref=ig_profile`을 붙이는 것만으로 이미 존재하는 `ref` 캡처 로직(`SajuFlow.tsx:44`)이 그대로 잡아줍니다. 코드 수정 0줄로 "인스타 프로필발 유입 수"를 즉시 분리 집계할 수 있는, 지금 당장 실행 가능한 가장 저렴한 개선입니다. 릴스마다 다른 값(`?ref=ig_r01` 등)을 넣으면 어떤 영상이 실제 방문까지 전환시키는지도 알 수 있습니다(단, 링크 서비스가 파라미터를 유지해주는지 확인 필요).

### ④~⑤ 랜딩 → 무료 입력 시작/완료
- **측정**: `saju_start ÷ landing_view`, `saju_complete ÷ saju_start`
- **현재 상태**: 랜딩에 도착하면 바로 입력 폼이 보이는 구조라 ④는 손실이 크지 않을 것으로 예상. ⑤(입력 시작 후 이탈)가 상대적으로 더 볼 만한 지표입니다.
- **개선 방법**: 만약 ⑤ 전환율이 낮게 나온다면(체감상 70% 미만), 생년월일시 입력 폼의 **필수 항목 개수**(이름/생년월일/시간/성별 등)가 원인일 가능성이 큽니다. 이번 요청 범위(사이트 기능 변경 금지)에서는 코드를 건드리지 않되, 데이터가 나오면 "시간 모름 체크박스"의 위치나 설명 문구 정도의 카피 개선은 추후 별도 요청으로 진행할 수 있습니다.

### ⑥ 무료 결과 열람
- 이미 자동으로 스크롤/렌더링되는 구간이라 사실상 100%에 가까운 것이 정상입니다. 여기서 손실이 보인다면 페이지 로딩 속도 문제일 가능성이 높으므로, 낮게 나올 경우에만 별도로 점검하면 됩니다.

### ⑦ 무료 결과 → 유료 미리보기 열람
- **현재 상태**: 이전 라운드(🔴1순위)에서 이미 "오늘의 운세(무료)"와 "심층 분석(유료)"의 차이를 헤드라인·서브헤드로 명확히 구분했고(`ctaCopy.ts`), 리뷰를 미리보기 바로 앞으로 옮겨 신뢰 신호를 CTA 근처에 배치했습니다(`ResultView.tsx`).
- **추가로 볼 지표**: `premium_preview_view` 자체는 스크롤만 하면 발생하므로, 여기서 중요한 건 **체류 시간**입니다(현재 이벤트로는 안 잡힘 — Vercel Analytics나 GA 스크롤 depth 트래킹을 쓰고 있다면 그쪽에서 보조 확인). 지금 이벤트만으로는 "봤다/안 봤다"만 구분되므로, 이 단계 자체보다는 다음 단계(⑧)의 전환율이 더 중요한 신호입니다.

### ⑧ 미리보기 → CTA 클릭 (가격을 처음 인지하는 지점 — 우선순위 최상)
- **측정**: `premium_cta_click ÷ premium_preview_view`
- **왜 중요한가**: `PremiumUnlock.tsx`의 `formStep`(intro→details) 구조상, 사용자가 **가격(4,900원)을 처음 인지하는 시점**이 정확히 이 클릭 버튼입니다. 즉 이 전환율이 낮다면 "가격 저항"인지 "가치 전달 실패"인지를 가르는 가장 직접적인 지표입니다.
- **개선 방법(카피 관점, 코드는 유지)**:
  - 현재 `PREMIUM_VALUE_CHECKLIST`(5개 항목)와 `PREMIUM_PREVIEW_VALUE_LINE`("6,000자 이상")이 버튼 위에 있습니다. 여기에 **가격 대비 가치를 숫자로 재확인**하는 한 줄("커피 한 잔 값으로 사주 전체를 8,600자 분량으로")을 버튼 바로 위, 클릭 직전 시선에 추가하는 카피 실험을 권장합니다(이미 3순위에서 가격 앵커링 문구를 넣었다면, 그 문구가 체크리스트와 버튼 사이 — 시선이 마지막으로 거치는 자리 — 에 있는지 위치만 재확인).
  - 버튼 문구 자체도 A/B 후보로 "상세 분석 시작하기"보다 "지금 확인하기"처럼 **행동+즉시성**을 강조하는 쪽이 통상 클릭률이 높습니다. 카피만 바꾸는 실험이라 결제 로직에는 영향 없습니다.

### ⑨ CTA 클릭 → 결제 시작(정보 입력)
- **측정**: `checkout_start ÷ premium_cta_click`
- **현재 상태**: `formStep="details"`로 넘어가면 이름/이메일/휴대폰/결제수단 입력 폼이 나옵니다. 이 단계 이탈은 "입력 항목이 많다"기보다 "정말 결제할 마음이 섰는가"를 재확인하는 구간이라, CTA 클릭 자체가 낮은 의도(호기심 클릭)였다면 여기서 자연스럽게 걸러집니다.
- **개선 방법**: 입력 폼 진입 시 이미 "‹ 이전" 버튼으로 되돌아갈 수 있어 이탈 자체는 막지 않는 게 맞는 설계입니다. 다만 폼 상단에 "결제 정보를 입력하고 바로 전체 분석을 받아보세요"(`PREMIUM_DETAILS_STEP_INTRO`) 문구가 있는데, 여기에 **소요 시간 안내**("30초면 끝나요") 한 줄을 더하면 입력 부담을 낮추는 데 도움이 됩니다.

### ⑩ 결제 시작 → 완료
- **측정**: `payment_success ÷ checkout_start`, 실패 시 `payment_fail` 사유는 PortOne 콘솔에서 확인
- 이 구간은 PG사 결제창 UX 영역이라 마케팅 카피로 개선할 여지가 거의 없습니다. `payment_fail` 비율이 유의미하게 높게 나올 때만(예: 특정 카드사 오류 집중) 별도로 PortOne 설정을 점검하면 됩니다 — 지금은 모니터링 대상으로만 표시합니다.

---

## 3. 지금 바로 실행 가능한 것 (코드 변경 없음)

1. **프로필 링크에 `?ref=ig_profile` 부착** — 기존 `ref` 캡처 로직을 그대로 활용해 "인스타 프로필발 방문"을 오늘부터 분리 집계 가능
2. **릴스별 `?ref=ig_r01` 같은 개별 파라미터**로 어떤 영상이 실제 방문 전환이 좋은지 확인(Day 10 체크포인트의 "저장+공유율"과 별개로, "방문 전환"이라는 더 하단 지표까지 확보)
3. **매주 위 SQL 쿼리로 이벤트 카운트 뽑아서 단계별 비율 기록** — 별도 대시보드 구축 없이 스프레드시트에 주 단위로 붙여넣기만 해도 추세 파악 가능
4. **⑧번 구간(미리보기→CTA클릭) 카피 A/B**를 다음 우선순위로 — 가격 인지 직전 지점이라 전환 임팩트가 가장 큼

## 4. 나중에 고려할 것 (지금은 손대지 않음)

- `AnalyticsEvent`에 `sessionId` 추가해 진짜 코호트 퍼널로 전환(방문자 규모가 커졌을 때)
- 관리자 대시보드(`/admin`)에 지금은 주문/후기만 있는데, 이벤트 카운트 요약 카드를 추가하면 매주 SQL을 직접 안 돌려도 됨 — 원하시면 별도로 작업 가능
