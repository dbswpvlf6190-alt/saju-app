# 이 파일은 값이 없는 템플릿입니다. 실제 값은 .env.local에만 넣으세요 (git에 커밋되지 않습니다).

# --- 데이터베이스 (필수) ---
# PostgreSQL 연결 문자열. Vercel + Neon Postgres 조합이면
# `vercel env pull` 로 자동으로 받아올 수 있습니다.
DATABASE_URL="postgresql://user:password@host/dbname?sslmode=require"

# --- 관리자 대시보드 (필수) ---
# /admin 로그인 비밀번호. 충분히 길고 무작위한 값으로 설정하세요.
ADMIN_PASSWORD=

# --- 주문 리포트 접근 토큰 (필수) ---
# GET /api/orders/[paymentId]가 paymentId만으로 조회되지 않도록, 주문 생성 시 발급하는
# 쿠키에 서명할 때 쓰는 비밀키입니다. `node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"`
# 로 생성하세요.
ORDER_ACCESS_SECRET=

# --- Claude API (유료 리포트의 AI 해석에 필요) ---
# https://console.anthropic.com → API Keys → Create Key
ANTHROPIC_API_KEY=

# --- PortOne(포트원) 결제 연동 (결제 기능에 필요) ---
# https://admin.portone.io → 결제 연동 → 채널 관리 / 식별코드·API Keys 에서 확인
# NEXT_PUBLIC_* 값은 브라우저에 그대로 노출되는 값이라 원래 공개돼도 되는 식별자입니다(비밀키 아님).
NEXT_PUBLIC_PORTONE_STORE_ID=
NEXT_PUBLIC_PORTONE_CHANNEL_KEY=
# 결제 검증(서버 전용)에 쓰는 값 - 절대 브라우저로 노출되면 안 됩니다.
PORTONE_API_SECRET=

# --- 카카오톡 공유 (결과 공유 기능에 필요) ---
# https://developers.kakao.com → 내 애플리케이션 → 앱 설정 → 앱 → 플랫폼 키 → JavaScript 키
# 앱 설정 → 플랫폼 키 → JavaScript 키에서 사이트 도메인(JS SDK 도메인)도 등록해야 동작합니다.
NEXT_PUBLIC_KAKAO_JS_KEY=
