# 사주랩 개인정보 처리 구조 전수 점검 (2026-08-30)

**범위**: 사주 계산 로직·결제 로직은 대상에서 제외하고, 개인정보 "수집·저장·전달·삭제" 구조만 점검했습니다. 코드는 전혀 수정하지 않았고, 아래 내용은 전부 실제 소스코드를 직접 읽고 DB를 직접 조회해서 확인한 사실입니다(추정 없음).

---

## 1. 데이터 흐름 지도 (수집 지점별)

| 수집 지점 | 수집 항목 | 전송/저장 위치 | 근거 |
|---|---|---|---|
| 무료 사주 (본인) | 생년월일시(선택)·성별·이름(선택) | **저장 안 됨** — 계산 후 응답만 반환, DB write 없음 | `src/app/api/saju/calculate/route.ts` |
| 무료 궁합 (본인+상대방) | 본인·상대방 생년월일시(선택)·성별, 이름(둘 다 선택) | **저장 안 됨** — 완전 무상태(stateless) 계산 | `src/app/api/saju/compatibility/route.ts` |
| "내 사주 다시 보기" | 본인 이름·생년월일시 | **브라우저 localStorage만**(서버 전송 없음) | `src/lib/revisit/localBirthInfo.ts:11` 주석에 명시, 실제로 서버 호출 없음 확인 |
| 궁합 결제 재개용 임시값 | 본인·상대방 이름 + 생년월일시 | **브라우저 sessionStorage만**(탭 닫으면 소멸) | `CompatibilityUnlock.tsx:141` |
| 유료 사주 주문 생성 | 본인 생년월일시·성별 | DB `Order.birthInputJson` (이름 없음) | `src/app/api/orders/route.ts:20-24` |
| 유료 궁합 주문 생성 | 본인+상대방 생년월일시·성별 (**이름은 요청 body에 아예 없음**) | DB `Order.birthInputJson`에 `{self, partner}` 형태로 저장 | `src/app/api/orders/route.ts:26-31`, `CompatibilityUnlock.tsx:146` (body에 `selfInput, partnerInput`만 전송, name 필드 없음) |
| 결제(포트원) | 결제자(본인) 이름·이메일·휴대폰 | **자사 DB에 저장 안 됨** — PortOne SDK 호출 시 `customer` 파라미터로만 전달, 서버로 별도 전송/저장 안 함 | `PremiumUnlock.tsx:197`, `CompatibilityUnlock.tsx:163` |
| AI 해석 생성(Anthropic) | 오행비율·일간·년주·성별·궁합점수 (구조화된 계산 결과만) | Anthropic API로 전달, **이름·연락처 없음** | `src/lib/ai/interpretCompatibility.ts:39-46,58-61` — `summarize()` 함수가 이름 없이 `본인_성별/일간/년주/오행비율` 형태로만 조립하는 것을 코드로 직접 확인 |
| 후기 | 별점·후기 내용 | DB `Review` (이름·연락처 필드 자체가 스키마에 없음) | `prisma/schema.prisma:39-47` |
| 이용 통계 | 이벤트명 + 비민감 meta | DB `AnalyticsEvent`, **생년월일·이름·연락처는 서버에서 강제 필터링** | `src/lib/analytics/events.ts:24-36` `FORBIDDEN_META_KEYS` 목록으로 코드 레벨 차단 확인 |

---

## 2. 궁합(제3자 정보) 흐름 상세 점검

요청하신 핵심 부분입니다. 실제 컴포넌트 코드를 끝까지 추적한 결과:

1. `CompatibilityForm.tsx`에서 본인/상대방 정보를 입력받는데, **이름은 둘 다 "선택" 항목**이고 `PersonBirthFields`에 `nameOptionalLabel="상대방 이름 (선택)"`으로 명시돼 있습니다(`CompatibilityForm.tsx:52`).
2. 무료 결과 계산(`/api/saju/compatibility`)에는 `selfInput`/`partnerInput`(생년월일시·성별)만 전송되고, **이름은 서버로 아예 전송되지 않습니다**(`CompatibilityFlow.tsx:18-22` — fetch body에 name 필드 없음). 이름은 화면 표시용으로만 클라이언트 상태에 남아있습니다.
3. 결제 시 주문 생성(`/api/orders`)도 body가 `{ productType, selfInput, partnerInput }`뿐이라 **상대방 이름은 서버에 도달하지 않습니다**(`CompatibilityUnlock.tsx:146`).
4. 결제 완료 후 DB에 남는 건 상대방의 **생년월일시(선택)·성별**뿐이며, 이게 `Order.birthInputJson`에 `{self:{...}, partner:{...}}` 형태로 저장되고 **전자상거래법 보존 규정에 따라 5년 보관**됩니다(`/privacy` 4항, 코드상으로도 별도 삭제 로직 없음 확인).
5. AI에게 전달되는 건 성별·일간·년주·오행비율뿐이라, 상대방을 실제로 특정할 수 있는 정보(이름 등)는 AI 프로바이더에도 전달되지 않습니다.
6. 현재 DB에 실제 궁합 결제 건은 아직 0건입니다(직접 조회 확인) — 위 구조는 코드 기준 분석이며 아직 실제 데이터로 검증된 사례는 없습니다.

**결론**: 상대방 "이름"은 이미 구조적으로 서버에 전혀 도달하지 않도록 설계돼 있습니다. 남는 문제는 **상대방의 생년월일시·성별이 결제 완료 시 5년간 원본 그대로 보관된다는 점**입니다.

---

## 3. 저장·보관·삭제 현황 요약

| 데이터 | 보관 기간 | 삭제 방식 |
|---|---|---|
| 무료 이용 입력값(본인/상대방 공통) | 즉시 파기(저장 안 함) | 해당 없음 |
| 유료 주문의 생년월일시(본인+궁합 상대방) | **5년**(전자상거래법 준용) | 자동 파기 로직 없음(코드상 확인) — 수동 관리 필요 |
| AI 해석 결과(aiResultJson) | 5년(주문과 함께) | 위와 동일 |
| 후기 | 삭제 요청 전까지 무기한 | 이메일/전화 문의 기반 수동 삭제만 존재, 셀프서비스 없음 |
| localStorage/sessionStorage | 이용자 기기에만, 이용자가 직접 삭제 가능 | 서버와 무관 |

---

## 4. 개인정보보호법 최소수집원칙 관점 위험 요소 (우선순위순)

### 🔴 R1. 궁합 상대방(제3자) 생년월일시·성별의 5년 보관은 목적 대비 과도
- **문제**: 전자상거래법이 요구하는 건 "대금결제 및 재화공급에 관한 기록"이지, 서비스 생성에 쓰인 원본 입력값 자체의 5년 보관이 아닙니다. 리포트(`aiResultJson`)는 결제 시점에 이미 생성·캐시되므로, 그 이후에는 원본 생년월일시(`birthInputJson`)가 "재화 공급"이나 "결제 확인" 목적에 더 이상 필요하지 않습니다.
- **특히 궁합의 경우**: 이 데이터의 정보주체(상대방)는 서비스 이용자가 아니고, 수집에 대해 스스로 동의하거나 인지할 방법이 전혀 없습니다. 목적 달성 후에도 계속 보관하는 건 개인정보보호법 제3조(수집 최소화)·제21조(목적 달성 시 파기) 원칙과 정면으로 배치될 소지가 있습니다.
- **영향받는 데이터**: 유료 궁합 주문의 `Order.birthInputJson.partner` 필드

### 🟡 R2. `birthInputJson`/`aiResultJson`이 평문 저장
- DB(Neon Postgres)는 전송/디스크 레벨 암호화는 되지만, 애플리케이션 레벨에서 별도 컬럼 암호화는 하지 않습니다. DB 접근 권한이 노출되면 생년월일시 원본이 그대로 보입니다.

### 🟡 R3. 상대방 이름이 sessionStorage에 일시적으로 남을 수 있음
- 서버로는 전송되지 않지만, 결제 재개(`PENDING_KEY`) 목적으로 브라우저에 `selfName`/`partnerName`이 잠깐 저장됩니다(`CompatibilityUnlock.tsx:141`). 탭을 닫으면 사라지지만, 공용 PC 환경에서 브라우저 세션 복구 등 예외적 상황이면 남을 가능성이 이론상 있습니다.

### 🟢 R4 (정보성, 문제 아님). 무료 이용 흐름은 이미 모범적으로 최소화되어 있음
- 무료 사주·무료 궁합 모두 완전 무상태 계산이라 저장 자체가 없고, 이건 최소수집원칙에 정확히 부합합니다. 유료 전환 시에만 데이터가 생기는 구조 자체는 바꿀 필요 없습니다.

### 🟢 R5 (참고). 자기정보 열람·삭제가 이메일/전화 문의 기반 수동 처리뿐
- 법적으로 필수는 아니지만, 처리 지연이나 누락 위험이 있습니다.

---

## 5. 개선안 (코드는 아직 반영하지 않음 — 제안만)

### 제안 1 (최우선) — 궁합 상대방 원본 생년월일시의 보관 기간 이원화
- **아이디어**: 결제 확인·재화공급 "기록"(상품명·금액·결제일시·상태)은 지금처럼 5년 유지하되, 리포트 생성에만 쓰인 **원본 생년월일시(특히 상대방분)는 결제 후 일정 기간(예: 90일, 재생성 지원 목적) 뒤 자동 파기하거나 비가역적으로 마스킹**(예: 일/시 정보 삭제하고 연도만 남김)합니다.
- **왜 안전한가**: 이미 생성된 `aiResultJson`(실제 리포트 본문)은 그대로 남아있어서, 이용자가 나중에 리포트를 다시 열람하는 데는 전혀 지장이 없습니다. 다만 "정확히 같은 입력으로 처음부터 재계산"은 불가능해지는데, 이 서비스는 어차피 결과를 캐시해서 재계산 자체를 안 하므로 실질적 영향이 없습니다.
- **트레이드오프**: 별도 배치 작업(만료된 주문의 `birthInputJson` 정리) 신규 구현이 필요합니다.

### 제안 2 — 상대방 이름 입력란에 안내 문구 추가
- 지금도 선택 입력이고 서버로 전송되진 않지만, "실명 대신 애칭이나 이니셜을 입력해도 괜찮아요" 같은 문구를 추가해 애초에 사용자가 실명을 입력하지 않도록 유도할 수 있습니다.

### 제안 3 — `birthInputJson` 컬럼 애플리케이션 레벨 암호화 검토
- DB 자체가 뚫리는 최악의 시나리오에서도 원본 생년월일시가 그대로 노출되지 않도록 하는 추가 방어선입니다. 우선순위는 R1보다 낮습니다(수집 최소화가 먼저, 암호화는 그다음 방어층).

### 제안 4 — 개인정보처리방침에 "상대방 정보 입력에 대한 이용자 책임" 문구 명확화
- 지금 방침에도 상대방 정보 수집 사실 자체는 고지돼 있지만, "상대방의 사전 동의를 받고 입력해야 한다"는 이용자 책임 문구를 추가하면 더 명확해집니다.

### 제안 5 (낮은 우선순위) — paymentId 기반 자기정보 셀프 열람/삭제 기능
- 법적 필수는 아니지만, 현재 이메일/전화 문의만 있는 삭제 요청 경로를 보완하는 차원의 장기 과제로 제안합니다.

---

## 6. 결론

전반적으로 **"수집하지 않는 것"에 대해서는 이미 상당히 잘 설계돼 있습니다** — 무료 흐름 완전 무상태, 결제자 이름/연락처 자사 DB 미저장, AI 전달 데이터에서 식별정보 배제, analytics 강제 필터링까지 코드 레벨에서 실제로 확인했습니다. 가장 시급히 손볼 지점은 **딱 하나, 궁합 상대방(제3자)의 원본 생년월일시가 목적 달성 이후에도 5년간 그대로 남아있는 보관 기간 설계**입니다. 진행 원하시면 제안 1부터 순서대로 코드에 반영해드리겠습니다.
