# 사주랩 공유 바이럴 전략 — 개인화 카드 기반

현재 구현된 카카오톡 공유 카드(`ShareButton.tsx`, `share.ts`, `/api/og/share`)를 그대로 활용해서, "내 결과 공유 → 친구가 궁금해서 클릭 → 친구도 입력 → 친구 결과 공유"로 이어지는 순환 구조를 강화하는 **카피·배치·콘텐츠 전략**입니다. 코드는 건드리지 않았고, 실행하려면 구현이 필요한 항목은 마지막에 따로 표시했습니다.

## 0. 개인정보 원칙 — 이미 지켜지고 있는 것 확인

코드에 이미 명시적으로 지켜지고 있는 원칙이라 이 문서의 모든 제안도 동일 기준을 따릅니다:
- 공유 카드(OG 이미지)·공유 텍스트·URL 어디에도 **이름·생년월일시·성별을 절대 넣지 않음** (`route.tsx`, `share.ts` 주석에 이미 명시)
- 노출되는 값은 **이미 화면에서 결과로 보여준 요약값**뿐: 일간 별명(예: "갑목"), 비유 문구, 궁합 점수. 이 자체가 "그 사람이 누구인지"를 특정할 수 없는 값이라 안전
- 아래 제안하는 모든 카피 변형도 이 범위(일간 별명/비유/점수/카테고리 요약) 안에서만 문구를 다르게 쓰는 것이지, 새로운 개인 데이터를 노출하지 않습니다

---

## 1. 현재 구조 진단

| 항목 | 현재 상태 |
|---|---|
| 공유 버튼 위치 | 무료 사주 결과 하단 1곳, 무료 궁합 결과 하단 1곳 — **총 2곳뿐** |
| 구매 후(유료 리포트) 공유 | **없음** — `PremiumUnlock.tsx`에 공유 버튼 자체가 없음 |
| 공유 유도 동기 | 버튼 문구 "💬 카카오톡으로 공유하기" — 기능 설명일 뿐 동기 부여 문구 아님 |
| 공유되는 콘텐츠 | 사주: 일간 별명+비유 한 줄 / 궁합: 점수+헤드라인 한 줄 |
| 트래킹 | `share_click` 이벤트 + `ref=share_kakao/native/copy`로 재유입 구분 가능 (이미 구현됨) |

**가장 큰 공백은 두 가지입니다**:
1. 공유가 "결과를 다 본 뒤 아래 있으니까 눌러본다"는 수동적 위치에만 있고, "공유해야 할 이유"를 만드는 카피가 없습니다.
2. 가장 콘텐츠가 풍부해서 자랑하고 싶은 순간(4,900원 결제 완료 직후)에는 공유 동선 자체가 없습니다.

---

## 2. 세 가지 바이럴 루프

### 루프 A — 사주 결과 공유 (호기심형, 1:다수)
지금 있는 구조. "내 일간이 뭐래" 하는 정체성 호기심이 트리거입니다. 약점: 궁합보다 "나만 보는 정보"라 친구를 끌어들일 이유가 약함.

### 루프 B — 궁합 결과 공유 (관계형, 1:1 — 가장 강력해야 하는데 지금 가장 약하게 쓰이고 있음)
궁합은 구조상 **입력한 사람과 상대방 두 명의 정보가 있어야 결과가 나오는** 서비스입니다. "내 애인/썸/친구와의 궁합"을 공유하면, 받는 사람은 단순 호기심이 아니라 **"내 얘기니까" 직접 확인하고 싶은 강한 동기**가 생깁니다. 이건 사주 앱류에서 가장 검증된 바이럴 패턴(예: 각종 궁합·MBTI 궁합 서비스)인데, 지금 카드 문구("우리 궁합은 87점, '...'래요")가 이 동기를 충분히 자극하지 못하고 있습니다.

### 루프 C — 구매 후 공유 (신뢰형, 지금 없음 → 신설 제안)
결제까지 한 사용자는 이미 서비스에 대한 신뢰와 만족도가 가장 높은 시점입니다. 이 순간 공유를 유도하면 "내가 실제로 써보니 괜찮더라"는 **가장 설득력 있는 추천 메시지**가 나갑니다. 지금은 이 순간에 공유 동선이 없어서 가장 좋은 재료를 못 쓰고 있는 셈입니다.

---

## 3. 카피 전략

### 3-1. 공유 버튼 자체의 문구 (동기부여형으로 교체)
지금: `💬 카카오톡으로 공유하기` (기능 설명)

제안 (배치될 위치별로 다르게):
- 무료 사주 결과 하단: `💬 내 사주, 친구는 뭐라고 나올까?` — 버튼을 누르는 행위가 "내 결과 자랑"이 아니라 "친구랑 비교해보고 싶은 호기심"으로 프레이밍됨
- 무료 궁합 결과 하단: `💬 이 결과 그 사람한테 보내기` — 궁합은 애초에 "보낼 대상"이 명확하므로 그 대상을 직접 지칭
- (신설) 구매 완료 직후: `💬 이 정도로 자세할 줄 몰랐어요 — 친구한테도 알려주기`

### 3-2. 카카오 공유 카드 본문(description) 문구 변형
지금 사주: `나의 사주는 갑목, "곧게 자라는 나무"래요. 무료로 내 사주도 확인해보세요 🔮`

제안 변형(궁금증 갭 강화 — 정보를 다 주지 않고 클릭을 유도):
- `나의 사주는 갑목이래요. 근데 이게 무슨 뜻인지 알아? 🔮 (30초, 무료)`
- `내 사주 키워드가 "곧게 자라는 나무"래... 소름. 너도 확인해봐 🔮`

지금 궁합: `우리 궁합은 87점, "..."래요. 무료로 우리 궁합도 확인해보세요 🔮`

제안 변형(받는 사람을 직접 지칭 — 관계 맥락 강화):
- `우리 궁합 87점 나왔는데, 뭐 때문에 이 점수 나왔는지 궁금하지 않아? 🔮`
- `우리 사이 궁합 봤는데... 너도 직접 봐야 알 듯 🔮`

카카오 SDK는 발신자가 대화방을 골라 보내는 구조라 "너"라는 2인칭이 자연스럽습니다 — 실제로 특정 친구/연인에게 전송되는 채널이라는 특성을 카피에 반영하는 것이 핵심입니다.

### 3-3. OG 카드 이미지(`/api/og/share`) 문구 — 궁금증 갭 원칙 적용
지금 헤드라인은 결과를 그대로 보여줍니다(`우리 궁합 87점`, `일간 별명`). 릴스 HOOK에 이미 적용한 "정보를 일부러 끊는다" 원칙(`CONTENT_PRODUCTION_GUIDE.md` 5번)을 카드에도 동일하게 적용할 수 있습니다:
- 궁합 카드 `sub` 문구를 결과 요약 대신 `"이 점수가 나온 진짜 이유는 따로 있어요"` 같은 궁금증 유발형으로 바꾸면, 이미지 자체가 클릭을 유도하는 힘이 커집니다(단, 이 변경은 `route.tsx`의 `sub` 렌더 로직은 그대로 두고 호출부에서 넘기는 문자열만 바꾸면 되는 카피 레벨 변경입니다)

### 3-4. 페르소나별 공유 유도 문구 (`TARGET_PERSONAS.md` 연계)
- **다인(썸/연애 페르소나)**: 궁합 결과가 이미 핵심 상품이므로 루프 B가 정확히 이 페르소나를 타깃. 공유 문구도 "썸/연애 상대에게 보낸다"는 맥락을 명시적으로 반영(위 3-2 궁합 변형)
- **지현(재물운)**: 결과보다 "이 정도로 맞다고?"라는 정확도 어필이 공유 동기 — `"내 소비 패턴 완전 저격당함... 너도 봐봐"`
- **준서(직업운)**: 공감형 — `"이거 완전 내 얘기인데? 회사 동료도 궁금해할 듯"`
- **유나(자기이해)**: 정체성 확인형 — 지금 사주 카드 기본 문구와 가장 잘 맞음, 그대로 유지

---

## 4. 배치 제안 — 공유 유도 시점을 3곳으로 확장

| 시점 | 현재 | 제안 |
|---|---|---|
| 무료 사주 결과 하단 | ✅ 있음 | 버튼 문구만 3-1 안으로 교체 |
| 무료 궁합 결과 하단 | ✅ 있음 | 버튼 문구 + 카드 문구를 3-2/3-3 안으로 교체(우선순위 최상 — 루프 B가 가장 강력함) |
| 결제 완료 직후(`PremiumUnlock.tsx` 언락 화면) | ❌ 없음 | **신설**: "📜 나의 사주 기반 심층 정보" 블록 하단에 `ShareButton` 추가. 문구는 3-1의 구매 후 변형 사용. 결제 완료 직후는 신뢰도가 가장 높은 순간이라 전환력 있는 추천이 나감 |

구매 후 공유는 **개인정보 노출 위험이 오히려 더 낮습니다** — 이미 화면에 나온 4대 카테고리 요약이나 오행 비율처럼, 무료 결과보다도 더 추상적인 값(예: "재물운 키워드: OO")만 카드에 담으면 되고, 유료 리포트 본문(1,200자 상세 해설)은 절대 카드에 노출하지 않는 원칙을 그대로 유지하면 됩니다.

---

## 5. 콘텐츠 전략 — Reels와 연결

이미 만들어둔 `INSTAGRAM_REELS_30.md`의 CTA 구간에 "친구 태그" 유도를 추가하면 앱 내 공유 루프와 릴스 유입 루프가 서로 강화됩니다:
- 궁합 관련 릴스(연애운 카테고리, R07/R08/R09~R12)의 캡션 마지막 줄에 `"썸 타는 그 사람 태그해서 같이 확인해봐 👇"` 같은 문구 추가 — 댓글 태그 자체가 알고리즘 노출을 늘리는 동시에, 태그된 친구가 앱에 방문할 확률도 올림
- 이 태그 유도는 영상 내 CTA 장면(가격 언급 없음 원칙 유지)이 아니라 **캡션에서만** 추가하는 것을 권장 — 영상 자체의 구조(`CONTENT_PRODUCTION_GUIDE.md`)는 이미 검증된 상태라 건드리지 않는 게 안전합니다

---

## 6. 측정 방법 (기존 트래킹 그대로 활용)

- `share_click` 이벤트 자체는 이미 위치 구분 없이 기록되므로, 신설되는 구매 후 공유 버튼도 같은 이벤트로 잡힘 — 다만 **어느 화면에서 눌렀는지 구분하려면** `trackEvent("share_click", { source: "free_result" | "premium" | "compat" })`처럼 `meta`에 위치 값을 추가하는 것을 권장(코드 한 줄 수준의 변경)
- 궁합 공유의 실제 바이럴 효과는 `landing_view`의 `ref=share_kakao` 건수 중 이후 `compatibility_start`로 이어지는 비율을 보면 확인 가능 — "궁합 공유를 받은 사람이 실제로 자기도 궁합을 넣어보는지"가 루프 B가 작동하는지의 핵심 신호

---

## 7. 실행 구분 — 카피만 vs 코드 변경 필요

| 항목 | 필요한 작업 |
|---|---|
| 공유 버튼 문구 교체(3-1) | 텍스트만 수정, `ShareButton` 사용처의 props 변경 |
| 카카오 카드 description 교체(3-2) | `ResultView.tsx`/`CompatibilityResultView.tsx`의 `text` prop 문자열 수정 |
| OG 카드 `sub` 문구 변경(3-3) | 호출부에서 넘기는 `card.sub` 문자열 수정(`route.tsx` 로직 변경 없음) |
| 구매 후 공유 버튼 신설(4번) | `PremiumUnlock.tsx`에 `ShareButton` 컴포넌트 추가 — 기존 컴포넌트 재사용이라 신규 로직 없음 |
| 릴스 캡션 태그 유도 문구(5번) | `INSTAGRAM_REELS_30.md` 캡션 텍스트만 수정 |
| `share_click`에 `source` meta 추가(6번) | 이벤트 이름 목록(`events.ts`)은 안 바꿔도 되고, 호출부 `meta` 인자만 확장 |

모두 기존 컴포넌트·로직을 그대로 쓰는 카피/배치 수준 변경이라 사주 계산·결제 로직·가격에는 영향이 없습니다. 진행 원하시면 우선순위(궁합 카드 문구 교체 → 구매 후 공유 신설 → 나머지)대로 순서대로 적용해드릴 수 있습니다.
