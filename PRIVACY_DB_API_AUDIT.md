# 사주랩 프로덕션 DB·API 개인정보 전수 검사 (2026-08-30)

`PRIVACY_AUDIT.md`(궁합 중심 서술형 점검)에 이어, 이번엔 **DB 스키마와 API 코드를 전부 훑어서 실제로 저장되는/저장될 가능성이 있는 개인정보를 항목별로 표로 정리**했습니다. 추측 없이 실제 소스와 DB 조회로만 확인했고, 코드는 수정하지 않았습니다.

**확인한 것**: `prisma/schema.prisma` 전체(4개 모델), 모든 `/api/**/route.ts`, `src/proxy.ts`(구 middleware), `src/lib/security/rateLimit.ts`, `src/lib/analytics/*`, `src/lib/revisit/localBirthInfo.ts`, `CompatibilityUnlock.tsx`/`PremiumUnlock.tsx`의 sessionStorage 사용처, DB 직접 쿼리(Order/Review/AnalyticsEvent 실제 건수).

---

## 1. 본인 생년월일·출생시간·성별 (무료 이용)

1. **데이터 항목**: 생년월일, 출생시간(선택), 성별, 양력/음력
2. **저장 DB 테이블/컬럼**: 없음
3. **언제 저장**: 저장되지 않음 — `/api/saju/calculate`가 요청받아 계산 후 응답만 반환(`src/app/api/saju/calculate/route.ts`), DB write 코드 자체가 없음
4. **언제 삭제**: 해당 없음
5. **왜 저장하는지**: 저장 안 함
6. **외부 서비스 전달**: 없음
7. **유출 위험도**: 없음(저장 자체가 없음)
8. **삭제/기간 단축 여지**: 해당 없음(이미 최소)

## 2. 본인 생년월일·출생시간·성별 (유료 주문)

1. **데이터 항목**: 생년월일, 출생시간(선택), 성별, 양력/음력
2. **저장 DB 테이블/컬럼**: `Order.birthInputJson` (String, JSON 직렬화)
3. **언제 저장**: 결제창을 열기 직전, 주문 생성 시점(`POST /api/orders`, `src/app/api/orders/route.ts:74-82`)
4. **언제 삭제**: 자동 삭제 로직 없음. `/privacy` 방침상 5년 보관 후 파기 예정이나, 실제 파기를 수행하는 배치/코드는 존재하지 않음(수동 관리 필요)
5. **왜 저장하는지**: 결제 완료 후 AI 리포트를 생성·재조회하기 위해 필요(재계산 근거)
6. **외부 서비스 전달**: Anthropic에는 원본 생년월일이 아니라 **계산된 결과값**(일간·년주·오행비율·성별)만 전달(`src/lib/ai/interpretSaju.ts` 확인). 원본 생년월일시 자체는 외부로 나가지 않음
7. **유출 위험도**: **중간** — 생년월일시는 민감도 있는 개인정보이나 그 자체로 특정 개인을 바로 식별하긴 어려움. DB 컬럼 자체는 암호화되어 있지 않음(Neon 전송/디스크 암호화는 적용되나 앱 레벨 컬럼 암호화 없음)
8. **삭제/기간 단축 가능 여부**: **가능**. 리포트(`aiResultJson`)가 이미 생성·캐시된 이후에는 원본 `birthInputJson`이 기능적으로 불필요 — 거래기록(금액·일시·상태)만 5년 유지하고 원본 생년월일은 더 짧은 기간(예: 90일) 후 파기/마스킹 가능

## 3. 궁합 상대방 생년월일·출생시간·성별 (무료 궁합)

1. **데이터 항목**: 상대방 생년월일, 출생시간(선택), 성별
2. **저장 DB 테이블/컬럼**: 없음
3. **언제 저장**: 저장되지 않음 — `/api/saju/compatibility`(`src/app/api/saju/compatibility/route.ts`)는 완전 무상태 계산, DB write 없음(코드 확인)
4. **언제 삭제**: 해당 없음
5. **왜 저장하는지**: 저장 안 함
6. **외부 서비스 전달**: 없음
7. **유출 위험도**: 없음
8. **삭제/기간 단축 여지**: 해당 없음(이미 최소)

## 4. 궁합 상대방 생년월일·출생시간·성별 (유료 궁합 주문) ⚠️ 중점 확인 항목

1. **데이터 항목**: 상대방 생년월일, 출생시간(선택), 성별 — **이름은 이 데이터에 포함되지 않음**(구조적으로 서버에 이름이 도달하지 않음, 아래 근거)
2. **저장 DB 테이블/컬럼**: `Order.birthInputJson` → `{ self: {...}, partner: {...} }` 형태(`src/app/api/orders/route.ts:18,26-31`)
3. **언제 저장**: 궁합 결제창을 열기 직전, 주문 생성 시점(`CompatibilityUnlock.tsx:143-147`) — 이 시점 요청 body에 `productType, selfInput, partnerInput`만 있고 이름 필드는 아예 없음(직접 코드 확인)
4. **언제 삭제**: 자동 삭제 로직 없음(2번 항목과 동일한 상태)
5. **왜 저장하는지**: 결제 완료 후 궁합 AI 리포트 생성·재조회용
6. **외부 서비스 전달**: Anthropic에는 `summarize()` 함수가 만든 `{성별, 일간, 년주, 오행비율}`만 전달(`src/lib/ai/interpretCompatibility.ts:39-46`) — 이름·원본 생년월일 모두 미전달을 코드로 직접 확인
7. **유출 위험도**: **중간~높음** — 이 데이터의 정보주체(상대방)는 서비스 이용자가 아니며 수집 사실을 인지하거나 동의할 방법이 전혀 없음. 실제 DB 조회 결과 **현재 이 유형의 실제 주문은 0건**(2026-08-30 기준, 직접 쿼리 확인)이라 아직 실피해는 없으나 구조적 위험은 유효
8. **삭제/기간 단축 가능 여부**: **가능, 최우선 권장** — 본인 데이터(2번)보다 더 강하게 권장. 상대방은 계약 당사자가 아니므로 전자상거래법상 보존 의무 논리가 본인만큼 명확히 적용되지 않음. 리포트 생성 후 원본 파기해도 서비스 제공에 지장 없음

## 5. 사주/궁합 AI 해석 결과

1. **데이터 항목**: AI가 생성한 사주/궁합 해석 텍스트 (최종 리포트 본문)
2. **저장 DB 테이블/컬럼**: `Order.aiResultJson` (String, nullable, JSON)
3. **언제 저장**: 결제 완료 후 리포트 최초 조회 시점에 섹션별로 생성되며 즉시 캐시(`src/app/api/orders/[paymentId]/route.ts:115-120`)
4. **언제 삭제**: 자동 삭제 없음, Order와 생명주기 동일(5년)
5. **왜 저장하는지**: 매 조회마다 AI를 재호출하지 않기 위한 캐시(비용·속도)
6. **외부 서비스 전달**: 없음(생성 시점에만 Anthropic 호출, 저장된 결과 자체는 재전달 안 됨)
7. **유출 위험도**: **낮음** — 텍스트 자체에 이름·연락처 등 식별정보가 없음(AI에 애초에 전달 안 됐으므로 출력에도 없음). 다만 "이 사람의 사주 해석"이라는 맥락 정보이긴 함
8. **삭제/기간 단축 가능 여지**: 원본 입력(2/4번)과 달리 이건 **오히려 유지가 나음** — 원본을 파기해도 이 캐시만 있으면 이용자에게 리포트를 계속 보여줄 수 있어서, 원본 삭제 전략(2/4번)이 성립하는 전제가 바로 이 캐시의 존재임

## 6. 주문 메타데이터 (결제 기록)

1. **데이터 항목**: `paymentId`(UUID), `status`, `amount`, `productType`, `createdAt`, `paidAt`
2. **저장 DB 테이블/컬럼**: `Order` 테이블 기본 컬럼 전부(`prisma/schema.prisma:16-28`)
3. **언제 저장**: 주문 생성 시(`createdAt`), 결제 확인 시(`paidAt`, `status→PAID`)(`src/app/api/orders/[paymentId]/complete/route.ts:59-62`)
4. **언제 삭제**: 자동 삭제 없음
5. **왜 저장하는지**: 전자상거래법상 결제 기록 보존 의무, 중복 결제 방지, 관리자 매출 확인(`/admin`)
6. **외부 서비스 전달**: `paymentId`가 PortOne 결제 조회 API 호출 시 사용됨(`src/lib/payment/verify.ts:24`) — PortOne은 이미 자체적으로 이 거래를 알고 있으므로 새로운 정보 전달이 아니라 조회
7. **유출 위험도**: **낮음** — 이 자체로는 특정 개인 식별 불가(이름·연락처 없음). 단, `paymentId`가 사실상 "이 리포트에 접근 가능한 열쇠" 역할을 함(7번 항목 참고)
8. **삭제/기간 단축 가능 여지**: 낮음(법적 보존 의무 대상이라 5년 유지가 타당)

## 7. 결제자 이름·이메일·휴대폰번호

1. **데이터 항목**: 결제자(본인) 이름, 이메일, 휴대폰번호
2. **저장 DB 테이블/컬럼**: **없음** — `Order` 스키마에 이름/이메일/전화 컬럼 자체가 존재하지 않음
3. **언제 저장**: 저장 안 됨. `PremiumUnlock.tsx:197`/`CompatibilityUnlock.tsx:163`에서 `@portone/browser-sdk/v2`의 `requestPayment()`를 **브라우저에서 직접 호출**하며 `customer` 파라미터로 넘기는데, 이 호출은 **브라우저 → PortOne** 경로라 우리 서버가 이 값을 아예 받지도 않음(우리 서버 API 어디에도 email/phone/fullName을 받는 엔드포인트가 없음을 grep으로 확인)
4. **언제 삭제**: 해당 없음(자사 보관 자체가 없음)
5. **왜 저장하는지**: 저장 안 함
6. **외부 서비스 전달**: **PortOne(및 제휴 PG사)에는 직접 전달됨**(브라우저→PortOne, 우리 서버 경유 안 함) — 결제 처리 및 결제 확인 메일 발송 목적
7. **유출 위험도**: 자사 기준 **없음**(보관 자체가 없음). PortOne 측 처리는 우리 통제 범위 밖(PG사 자체 개인정보처리방침 적용 대상)
8. **삭제/기간 단축 여지**: 해당 없음(이미 미보관)

## 8. 이용 후기

1. **데이터 항목**: 별점, 후기 내용, 작성일
2. **저장 DB 테이블/컬럼**: `Review.rating`, `Review.content`, `Review.createdAt`, `Review.paymentId`, `Review.productType`, `Review.visible`
3. **언제 저장**: 결제 완료 주문에 대해 이용자가 후기 작성 시(`ReviewForm` → `/api/reviews`)
4. **언제 삭제**: 삭제 요청(이메일/전화 문의) 시 수동 처리만 존재. 관리자 페이지에서 "숨김(visible=false)" 처리는 가능하나 완전 삭제 UI는 없음(`src/components/admin/AdminReviewToggle.tsx` 확인 결과 visible 토글만 존재)
5. **왜 저장하는지**: 실제 구매자 후기 게시(사회적 증거)
6. **외부 서비스 전달**: 없음
7. **유출 위험도**: **낮음** — 스키마에 이름·연락처 필드 자체가 없음. 다만 후기 **본문(content)에 이용자가 자발적으로 실명이나 식별 정보를 적어 넣을 가능성**은 남아있음(입력 제한 없음)
8. **삭제/기간 단축 가능 여지**: 완전 삭제(hard delete) 기능을 관리자 페이지에 추가하면 개선 가능(현재는 숨김만 가능)

## 9. Analytics 이벤트

1. **데이터 항목**: 이벤트명(`landing_view` 등 15종 고정), 선택적 meta(예: `ref`, `productType`, `rating`, `source`)
2. **저장 DB 테이블/컬럼**: `AnalyticsEvent.name`, `AnalyticsEvent.metaJson`, `AnalyticsEvent.createdAt`
3. **언제 저장**: 사용자 행동 시점마다(`POST /api/analytics/event`)
4. **언제 삭제**: 자동 삭제 없음
5. **왜 저장하는지**: 전환 퍼널 분석
6. **외부 서비스 전달**: 없음
7. **유출 위험도**: **낮음** — `FORBIDDEN_META_KEYS`(`src/lib/analytics/events.ts:24-36`)가 `year/month/day/hour/minute/gender/birthinput/name/email/phone/phonenumber`를 **클라이언트·서버 양쪽에서 이중 필터링**하는 걸 코드로 확인. IP·User-Agent도 별도로 저장하지 않음(라우트 코드에 그런 필드 자체가 없음)
8. **삭제/기간 단축 여지**: 낮음(이미 비식별 데이터 위주). 장기 보관 시 용량 관리 차원에서 오래된 이벤트 주기적 정리는 고려 가능하나 개인정보 이슈는 아님

## 10. URL 파라미터 — `ref`

1. **데이터 항목**: 유입 경로 식별자(`share_kakao`, `share_native`, `share_copy`, `ig_profile` 등)
2. **저장 DB 테이블/컬럼**: `AnalyticsEvent.metaJson`의 `ref` 필드로 저장(`SajuFlow.tsx:44-45`)
3. **언제 저장**: 랜딩 페이지 최초 진입 시 URL에서 읽어 즉시 히스토리에서 제거(`history.replaceState`) 후 이벤트로만 남김
4. **언제 삭제**: AnalyticsEvent와 생명주기 동일
5. **왜 저장하는지**: 마케팅 채널별 유입 효과 측정
6. **외부 서비스 전달**: 없음
7. **유출 위험도**: **없음** — 개인 식별 정보 아님(채널 이름표일 뿐)
8. **삭제/기간 단축 여지**: 해당 없음

## 11. URL 파라미터 — `paymentId`

1. **데이터 항목**: 주문 고유 식별자(UUID)
2. **저장 DB 테이블/컬럼**: 저장 자체는 `Order.paymentId`(이미 6번에 포함), URL에는 결제창 리디렉션 복귀 시(`?paymentId=...`) 노출됨
3. **언제 저장**: URL 자체는 저장 안 됨(브라우저 히스토리에서 즉시 제거, `SajuFlow.tsx:53`/`CompatibilityFlow.tsx:45`)
4. **언제 삭제**: 즉시(replaceState로 URL에서 제거)
5. **왜 URL에 노출**: 모바일 결제창은 리디렉션 방식이라 복귀 시 어떤 주문인지 식별할 값이 필요
6. **외부 서비스 전달**: PortOne이 리디렉션 URL을 생성(우리가 지정한 `redirectUrl` 기준)
7. **유출 위험도**: **중간** — `paymentId`는 사실상 "이 리포트를 열람할 수 있는 열쇠"입니다. `GET /api/orders/[paymentId]`(`src/app/api/orders/[paymentId]/route.ts:29`)에 **별도 인증이 없어서**, paymentId를 아는 사람은 누구나 해당 주문의 AI 리포트 섹션을 조회할 수 있습니다. UUID라 추측은 사실상 불가능하지만, 브라우저 히스토리 동기화·스크린샷 공유·리퍼러 로그 등으로 우연히 노출되면 제3자가 리포트를 열람할 수 있는 구조입니다.
8. **삭제/기간 단축 여지**: 해당 없음(단축 문제가 아니라 접근 통제 설계의 문제 — 개선안 참고)

## 12. localStorage — `saju:lastBirthInfo`

1. **데이터 항목**: 이름, 생년월일시, 성별 (마지막으로 조회한 본인 정보)
2. **저장 위치**: 서버 아님 — **이용자 브라우저의 localStorage**(`src/lib/revisit/localBirthInfo.ts:14-22`)
3. **언제 저장**: "내 사주 다시 보기" 편의 기능 사용 시
4. **언제 삭제**: 이용자가 브라우저에서 직접 삭제하기 전까지 영구 보관(기기 로컬)
5. **왜 저장하는지**: 재방문 시 재입력 없이 결과를 다시 볼 수 있게 하는 편의 기능
6. **외부 서비스 전달**: 없음(서버로 전송된 적 없음, 코드에 fetch 호출 없음을 확인)
7. **유출 위험도**: **낮음** — 이용자 본인 기기에만 존재. 공용 컴퓨터를 쓰는 경우 다음 사용자에게 노출될 여지는 있음(이용자 책임 영역)
8. **삭제/기간 단축 여지**: 서버 관리 대상이 아니므로 해당 없음. 다만 방침에 "브라우저에서 직접 삭제 가능"이라는 안내가 이미 존재(`/privacy` 6항)

## 13. sessionStorage — 결제 재개용 임시값

1. **데이터 항목**: (사주) 이름+생년월일시 / (궁합) 본인·상대방 이름+생년월일시
2. **저장 위치**: 이용자 브라우저의 sessionStorage(`saju:pendingPurchase`, `saju:pendingCompatibilityPurchase`)
3. **언제 저장**: 결제창을 여는 시점(`PremiumUnlock.tsx:172-175`, `CompatibilityUnlock.tsx:141`)
4. **언제 삭제**: 결제 완료 후 코드에서 명시적으로 제거(`sessionStorage.removeItem(PENDING_KEY)`, `PremiumUnlock.tsx:85`/`CompatibilityUnlock.tsx:56`), 또는 탭을 닫으면 브라우저가 자동 소멸
5. **왜 저장하는지**: 모바일 결제창 리디렉션 복귀 시 결과 화면을 다시 계산하기 위한 임시 값
6. **외부 서비스 전달**: 없음
7. **유출 위험도**: **낮음** — 서버 미전송, 탭 종료 시 자동 소멸. 결제 실패 등으로 `removeItem`이 호출 안 되는 예외 경로가 있으면 탭이 열려있는 동안은 남아있을 수 있음
8. **삭제/기간 단축 여지**: 이미 최소 수준(명시적 삭제 + 자동 소멸 이중 장치)

## 14. 관리자 인증 쿠키

1. **데이터 항목**: `saju_admin_auth` — `ADMIN_PASSWORD`를 비밀키로 한 HMAC 서명값(고정 문자열 "admin-authenticated"의 서명)
2. **저장 위치**: 브라우저 쿠키(서버 DB 아님)
3. **언제 저장**: 관리자 로그인 성공 시(`src/app/admin/login/actions.ts:30-36`)
4. **언제 삭제**: `maxAge: 60*60*8`(8시간) 후 자동 만료, 로그아웃 기능은 별도로 없음(수동 쿠키 삭제 필요)
5. **왜 저장하는지**: 관리자 페이지(주문/후기 조회) 접근 인증
6. **외부 서비스 전달**: 없음
7. **유출 위험도**: **낮음** — 값 자체가 이용자 개인정보가 아니라 관리자 세션 토큰. `httpOnly`, 프로덕션에서 `secure` 적용 확인(`src/lib/admin/auth.ts` 및 `actions.ts:31-36`)
8. **삭제/기간 단축 여지**: 8시간이면 이미 적절. 로그아웃 버튼 부재는 개인정보보다는 UX 개선 사항

## 15. 서버 로그(console.error 등)

1. **데이터 항목**: 에러 메시지 + `paymentId`
2. **저장 위치**: Vercel 플랫폼 로그(코드가 저장하는 게 아니라 런타임 stdout이 자동 수집됨)
3. **언제 저장**: 예외 발생 시(`grep`으로 전체 `console.error` 호출부 확인 결과, birthInput/selfInput/partnerInput 원본 값을 로그에 찍는 코드는 **없음** — 전부 `paymentId`나 일반 에러 객체만 로깅)
4. **언제 삭제**: Vercel 로그 보관 정책에 따름(플랜별 상이, 우리 코드가 통제하는 영역 아님)
5. **왜 저장하는지**: 장애 대응
6. **외부 서비스 전달**: Vercel(호스팅사)
7. **유출 위험도**: **낮음** — 실제 개인정보(생년월일·이름 등) 원본이 로그에 찍히는 코드 경로를 찾지 못함
8. **삭제/기간 단축 여지**: Vercel 플랫폼 설정 영역, 앱 코드 개선 대상 아님

## 16. Rate Limit용 IP 주소

1. **데이터 항목**: 요청자 IP 주소(`x-forwarded-for` 헤더)
2. **저장 위치**: **서버 메모리에만**(`src/lib/security/rateLimit.ts:12` — `Map` 객체, DB 아님)
3. **언제 저장**: API 호출마다 임시로
4. **언제 삭제**: `windowMs` 경과 후 자동 만료 로직 있음(같은 파일:22-26), 서버 재시작/재배포 시에도 전부 소멸
5. **왜 저장하는지**: 무차별 대입·스팸 방지
6. **외부 서비스 전달**: 없음
7. **유출 위험도**: **낮음** — 비영구, DB 미저장, 코드 주석에도 명시(`rateLimit.ts:8-11`)됐듯 프로덕션 스케일 시 개선 필요한 건 보안 강도이지 개인정보 이슈가 아님
8. **삭제/기간 단축 여지**: 이미 최소(수 분~수십 분 단위 자동 만료)

## 17. (참고, 범위 외) KakaoToken

`KakaoToken` 테이블은 **사주랩 최종 이용자의 개인정보가 아니라**, 결제 알림용으로 서비스 운영자 본인 계정에 발급된 OAuth 토큰입니다(별도 세션에서 구축한 카카오 알림 기능). 이용자 데이터 흐름과 무관해 이번 점검 범위에서는 참고용으로만 표기합니다.

---

## 요약 — 위험도별 정리

| 위험도 | 항목 |
|---|---|
| 🔴 중간~높음 | 4. 궁합 상대방 생년월일시(유료 주문) |
| 🟡 중간 | 2. 본인 생년월일시(유료 주문), 11. `paymentId` 접근 통제 부재 |
| 🟢 낮음 | 5,6,8,9,12,13,14,15,16 |
| ⚪ 없음(미보관) | 1,3,7,10 |

**가장 먼저 손볼 두 가지**: (1) 궁합 상대방 원본 생년월일시의 보관기간 단축(`PRIVACY_AUDIT.md` 제안 1과 동일), (2) `GET /api/orders/[paymentId]`에 대한 접근 통제 보강(예: 결제 시 입력한 이메일 뒷자리 확인, 또는 세션 기반 1회성 접근 토큰 등 — 현재는 UUID를 아는 것만으로 리포트 열람 가능).

코드 반영 원하시면 순서를 정해서 진행하겠습니다.
